// Shared capsule styling for the bottom-bar controls (menu + filter triggers)
// so they read as the same component. Hover inverts to dark.
export const CAPSULE =
  "group flex items-center justify-center gap-2.5 overflow-hidden whitespace-nowrap rounded-lg border border-dark/15 bg-canvas px-4 py-2.5 text-sm font-medium text-dark transition-colors duration-300 hover:border-dark/40 hover:bg-dark hover:text-white"

// Round "X" button shown when a bottom-bar panel (menu / filter) is open.
export const ROUND_CLOSE =
  "flex h-[42px] w-[42px] shrink-0 items-center justify-center rounded-full border border-dark/15 bg-canvas text-dark transition-colors duration-300 hover:border-dark hover:bg-dark hover:text-white"
